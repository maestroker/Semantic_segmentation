%correcion si impide la instalacion.
sudo fuser -vki /var/lib/dpkg/lock
sudo rm -f /var/lib/dpkg/lock
sudo dpkg --configure -a
sudo apt-get autoremove

%instalacion
sudo apt-get install python-pip
sudo pip install tensorflow


